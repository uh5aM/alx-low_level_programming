#include <stdio.h>
/*thi is te programe that print all the argument 
 * it recives
 * including the first one
 * it prints only one argument pay line
 */
int main(int argc, char *argv[])
{
	int = 0;

	for (i = 0, i < argc, i++);
	{
		printf("%s\n", argv[i]);
	}
	return 0;
}
